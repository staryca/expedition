{% if app.user %}
    <a class="nav-link" href="{{ path('user_profile') }}">{{ app.user.username }}</a>
{% else %}
    <a class="nav-link" href="{{ path('hwi_oauth_service_redirect', {'service': 'google' }) }}">Sign In</a>
{% endif %}
