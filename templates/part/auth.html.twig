{% if app.user %}
    <a class="nav-link" href="{{ path('user_profile') }}" title="{{ app.user.gelFullName() }}"><i class="bi bi-person"></i> {{ app.user.firstName }}</a>
{% else %}
    <a class="nav-link" href="{{ path('hwi_oauth_service_redirect', {'service': 'google' }) }}">Увайсці</a>
{% endif %}
